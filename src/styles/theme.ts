import { Colors } from "./colors"
import { Spacing } from "./spacing"

export const Theme = {
  colors: Colors,
  spacing: Spacing,
  borderRadius: 8,
  text: {
    fontFamily: 'Figtree-Regular',
    fontSize: 18,
  }
}
